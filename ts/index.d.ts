export declare class Remarkd {
  parse(markdown: string, detectHeaders?: boolean): string;
  static parse(markdown: string, detectHeaders?: boolean): string;
}

export declare function parse(markdown: string, detectHeaders?: boolean): string;

export default Remarkd;
